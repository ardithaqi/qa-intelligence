BASE_URL=https://your-app.example.com
HEADLESS=true
PW_WORKERS=2
PW_RETRIES=1

# Optional — enable AI failure analysis in teardown
# AI_ANALYSIS=true
# AI_PROVIDER=openai          # openai | anthropic | openai-compatible
# AI_MODEL=gpt-4o-mini        # provider-specific default if unset
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# AI_API_KEY=...              # generic fallback for any provider
# AI_BASE_URL=...             # required for openai-compatible (Ollama, Azure, etc.)
